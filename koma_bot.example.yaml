debug: false
address: localhost:8000
sounds:
  dir: "/etc/koma_bot/sounds"
twitter:
  login:
    consumer_key:
    consumer_secret:
    access_token_key:
    access_token_secret:
  track: "#hashtag"
  stream1:
    Hashtags:
      - "hashtag"
    Users:
      - "user"
  stream2:
    Hashtags:
      - "hashtag"
    Users:
      - "user"
